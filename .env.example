# Copy this to .env and adjust as needed. docker-compose.yml already has
# sane defaults baked in, so .env is optional for local use.

SECRET_KEY=change-this-in-production
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
POSTGRES_DB=studentportal
